version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

python:
  install:
    - method: uv
      command: pip
      requirements: requirements-docs.txt

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true
